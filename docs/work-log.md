# Work Log

| # | Name | Date | Description | Status | Folders |
|---|------|------|-------------|--------|---------|
| 1 | Auto-delete deletable chats | 2026-07-15 | Added one-click Auto Delete to popup and overlay. Scans sidebar, selects all chats matching filter config, confirms once, and deletes them automatically. Added `confirmBeforeDelete` setting (default true) for safety. | Completed | src/shared, src/content, src/popup, docs |
| 2 | Fix deletion click simulation | 2026-07-15 | Replaced simple `.click()` with full pointer + mouse event sequence (`pointerover`, `pointerenter`, `pointerdown`, `mousedown`, `pointerup`, `mouseup`, `click`) plus native click fallback. This matches React event expectations and should make the delete/confirm clicks register. Increased settle delays between steps. | Completed | src/content/deleter.ts |
| 3 | Fix confirm-button mis-click | 2026-07-15 | Logs showed the script found a "dialog" (actually a menu popover), failed to find a confirm button inside it, then fell back globally and clicked the original **Delete** menu item instead of the dialog's confirm button. Tightened `findConfirmDialog` to prefer `role="dialog"[aria-modal="true"]` and avoid menus. Added visibility checks so hidden menu items aren't clicked. Restricted confirm-button search to inside the found dialog. | Completed | src/content/selectors.ts |
| 4 | Harden auto-delete against ChatGPT UI changes | 2026-07-15 | Auto-delete reported broken after ChatGPT UI updates. Hovered-parent traversal, right-click context-menu fallback, broader `data-testid`/aria-label selectors, more flexible delete/confirm text matching, and visibility guards. Rebuilt extension; lint clean for touched files. | Completed | src/content/selectors.ts, src/content/deleter.ts, dist |
| 5 | Fix normal delete and add API fallback | 2026-07-15 | Normal delete also reported broken. Added backend-API deletion via `src/content/api.ts` as the primary method: fetches `/api/auth/session` for bearer token, then `DELETE /backend-api/conversation/{id}`. DOM simulation is now a fallback. Added per-step error reasons (`currentChatError`) to `DeletionProgress` and surfaced them in popup/overlay status. Rebuilt; lint clean. | Completed | src/content/api.ts, src/content/deleter.ts, src/shared/types.ts, src/popup/App.tsx, src/content/overlay.ts, docs |
| 6 | Add deletion progress visualizer and cancel button | 2026-07-15 | Added real-time progress visualization in both popup and overlay: percentage, completed/failed/remaining counts, current chat name, error display, and progress bar. Added cancel button to stop deletion mid-run via `CANCEL_DELETE` message. Content script now broadcasts `DELETE_PROGRESS` and `DELETE_COMPLETE` messages. Enhanced CSS for progress card. | Completed | src/content/index.ts, src/content/overlay.ts, src/popup/App.tsx, src/shared/types.ts, src/index.css, docs |
| 7 | Add 2-second delay between requests | 2026-07-15 | Changed default deletion delay from 1500ms to 2000ms across all files. Added explicit 2-second sleep after each API call in `deleteChat`. Increased inter-iteration pause to 500ms for total ~2.5s between requests. Updated all hardcoded delay values. | Completed | src/content/deleter.ts, src/content/index.ts, src/content/overlay.ts, src/popup/App.tsx, src/shared/types.ts, docs |
| 8 | Add anti-rate-limit: 5s delay, exponential backoff, jitter | 2026-07-15 | Changed default delay to 5s. Added exponential backoff with jitter in API calls (retries 3x: 5s, 10s, 20s). Added jitter (±20%) to all delays. Added `isRateLimited()` detection (HTTP 429/403/400). Inter-iteration pause increased to 1.5s with jitter. | Completed | src/content/api.ts, src/content/deleter.ts, src/content/index.ts, src/content/overlay.ts, src/popup/App.tsx, src/shared/types.ts, docs |
